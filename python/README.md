# Python受信クライアント

WebSocket経由でwebcam映像を受信して表示するPythonクライアントです。

## セットアップ

```bash
pip install -r requirements.txt
```

## 使用方法

```bash
python receiver.py [ws://127.0.0.1:8080/ws]
```

デフォルトでは `ws://127.0.0.1:8080/ws` に接続します。

## 操作方法

- `q`キー: 終了

